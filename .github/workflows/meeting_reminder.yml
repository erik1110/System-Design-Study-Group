name: Meeting Reminder
on:
  schedule:
    - cron: '40 13 * * 5'
  workflow_dispatch:

jobs:
  send_discord_message:
    runs-on: ubuntu-latest
    steps:
      - name: Calculate UK time
        id: uktime
        run: |
          MONTH=$(date -u +%-m)
          if [ "$MONTH" -ge 3 ] && [ "$MONTH" -le 10 ]; then
            echo "uk=14:00 BST (UTC+1)" >> $GITHUB_OUTPUT
          else
            echo "uk=13:00 GMT (UTC+0)" >> $GITHUB_OUTPUT
          fi

      - name: Send Discord Webhook Message
        uses: tsickert/discord-webhook@v7.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          username: Pikachu Assistant
          allowed-mentions: '{"parse": ["everyone"]}'
          content: |
            📢 **Weekly Meeting Reminder!**
            @everyone
            Tonight's weekly meeting starts at:
            🇬🇧 UK: ${{ steps.uktime.outputs.uk }}
            🇹🇼 Taiwan: 21:00 TST (UTC+8)
            🇯🇵 Japan: 22:00 JST (UTC+9)
            Please be on time! 🙏
